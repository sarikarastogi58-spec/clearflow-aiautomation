import { Dashboard } from "./dashboard";

export const metadata = {
  title: "ClearFlow — Clear Web Solutions",
  description: "AI lead generation, customer support, outreach and conversion operations for Clear Web Solutions.",
};

export default function Home() {
  return <Dashboard />;
}
